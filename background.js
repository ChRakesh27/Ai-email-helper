// background.js - currently small but useful for future features (alarms, central messaging, API proxy, oauth, etc.)
chrome.runtime.onInstalled.addListener(() => {
  console.log("AI Email Helper installed.");
});
